**实验一：机器学习开发平台搭建与算法测试实验任务书（约4学时）**

**一、实验目的**

1. 掌握在Windows和Linux操作系统下搭建机器学习开发环境的技能

2. 熟悉主流机器学习算法的原理和实现方法

3. 建立完整的机器学习项目开发、测试和评估流程

4. 比较不同算法在相同数据集上的性能表现

**二、实验环境要求**

**1. Windows环境配置**

操作系统: Windows 10/11 64位

Python环境: Python 3.8+

开发工具:

PyCharm 或 VS Code

Jupyter Notebook

必要库:

基础科学计算: numpy, scipy, pandas

可视化: matplotlib, seaborn, plotly

机器学习: scikit-learn, xgboost, lightgbm

深度学习: tensorflow, pytorch, keras

其他: jupyter, notebook, ipykernel

**2. Linux环境配置**

操作系统: Ubuntu 20.04 LTS 或 CentOS 8

Python环境: Python 3.8+ (建议使用conda环境)

- 开发工具:

- VS Code 或 Vim/Emacs

- Jupyter Lab

- 必要库: 同Windows环境要求

**三、实验内容与步骤**

**1. 环境搭建**

**任务1.1：Windows环境搭建**

**创建conda环境**

conda create -n ml\_env python=3.9

conda activate ml\_env

**安装基础包**

pip install numpy scipy pandas matplotlib seaborn

pip install jupyter notebook jupyterlab

**安装机器学习库**

pip install scikit-learn xgboost lightgbm

pip install tensorflow torch torchvision torchaudio

**任务1.2：Linux环境搭建**

**Ubuntu系统更新**

sudo apt update && sudo apt upgrade -y

**安装miniconda**

wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86\_64.sh

bash Miniconda3-latest-Linux-x86\_64.sh

**环境配置（同Windows）**

**任务1.3：环境验证**

创建验证脚本 environment\_test.py，测试所有库是否正确安装。

**2. 算法实现与测试**

**任务2.1：数据准备**

- 使用scikit-learn内置数据集（iris, digits, breast\_cancer）

- 准备外部数据集（MNIST, CIFAR-10, Titanic）

- 数据预处理和特征工程

**任务2.2：传统机器学习算法实现**

**k近邻算法**

from sklearn.neighbors import KNeighborsClassifier

from sklearn.model\_selection import cross\_val\_score

def test\_knn(X, y):

knn = KNeighborsClassifier(n\_neighbors=5)

scores = cross\_val\_score(knn, X, y, cv=5)

return scores.mean()

**线性回归与逻辑回归**

from sklearn.linear\_model import LinearRegression, LogisticRegression

from sklearn.metrics import accuracy\_score, mean\_squared\_error

def test\_linear\_models(X, y, problem\_type='classification'):

if problem\_type == 'classification':

model = LogisticRegression()

metric = accuracy\_score

else:

model = LinearRegression()

metric = mean\_squared\_error

实现训练和评估逻辑

**决策树与贝叶斯分类**

from sklearn.tree import DecisionTreeClassifier

from sklearn.naive\_bayes import GaussianNB

def test\_tree\_bayes(X, y):

dt = DecisionTreeClassifier(max\_depth=5)

nb = GaussianNB()

比较两种算法性能

**任务2.3：支持向量机与聚类**

from sklearn.svm import SVC

from sklearn.cluster import KMeans

from sklearn.metrics import silhouette\_score

def test\_svm\_clustering(X, y):

svm = SVC(kernel='rbf')

kmeans = KMeans(n\_clusters=3)

实现算法测试

**任务2.4：深度神经网络**

import tensorflow as tf

from tensorflow.keras.models import Sequential

from tensorflow.keras.layers import Dense

def create\_dnn\_model(input\_dim, output\_dim):

model = Sequential([

Dense(128, activation='relu', input\_shape=(input\_dim,)),

Dense(64, activation='relu'),

Dense(output\_dim, activation='softmax')

])

model.compile(optimizer='adam',

loss='sparse\_categorical\_crossentropy',

metrics=['accuracy'])

return model

**任务2.5：集成学习**

from sklearn.ensemble import RandomForestClassifier, AdaBoostClassifier

from sklearn.ensemble import VotingClassifier

def test\_ensemble\_methods(X, y):

rf = RandomForestClassifier(n\_estimators=100)

ada = AdaBoostClassifier(n\_estimators=50)

voting\_clf = VotingClassifier(

estimators=[('rf', rf), ('ada', ada)],

voting='soft'

)

比较个体学习器与集成学习器性能

**3. 性能评估与比较（可选）**

**任务3.1：统一评估框架**

创建统一的评估函数，对所有算法进行公平比较：

python

def evaluate\_model(model, X\_train, X\_test, y\_train, y\_test):

统一评估机器学习模型性能

start\_time = time.time()

model.fit(X\_train, y\_train)

training\_time = time.time() - start\_time

start\_time = time.time()

y\_pred = model.predict(X\_test)

prediction\_time = time.time() - start\_time

accuracy = accuracy\_score(y\_test, y\_pred)

precision = precision\_score(y\_test, y\_pred, average='weighted')

recall = recall\_score(y\_test, y\_pred, average='weighted')

f1 = f1\_score(y\_test, y\_pred, average='weighted')

return {

'accuracy': accuracy,

'precision': precision,

'recall': recall,

'f1\_score': f1,

'training\_time': training\_time,

'prediction\_time': prediction\_time

}

**任务3.2：结果可视化**

创建结果比较图表：

- 算法精度对比柱状图

- 训练时间对比图

- 学习曲线和验证曲线

- 混淆矩阵热力图

**任务3.3：跨平台性能测试**

在Windows和Linux环境下运行相同的测试代码，比较：

- 训练速度差异

- 内存使用情况

- 算法精度一致性

**四、实验交付物**

**1. 代码文件**

- environment\_setup.sh / environment\_setup.bat - 环境配置脚本

- data\_preprocessing.py - 数据预处理模块

- algorithm\_implementations.py - 算法实现

- model\_evaluation.py - 评估框架

- visualization.py - 结果可视化

- main.py - 主程序入口

**2. 文档报告**

- 实验环境搭建文档：详细记录环境配置步骤和遇到的问题

- 算法实现说明：每个算法的原理、参数调优过程

- 性能分析报告：各算法在不同数据集上的表现对比

- 跨平台比较报告：Windows与Linux环境下的性能差异分析

**3. 实验总结**

- 关键技术难点和解决方案

- 实验收获和心得体会

- 后续改进方向

**五、评分标准**

|  |  |  |
| --- | --- | --- |
| **评分项** | **权重** | **评分标准** |
| 环境搭建完整性 | 20% | 双系统环境成功搭建，所有依赖库正常使用 |
| 算法实现正确性 | 30% | 8种算法正确实现，代码规范可读 |
| 实验结果分析 | 25% | 性能评估全面，对比分析深入 |
| 跨平台测试 | 15% | 双环境测试完整，差异分析合理 |
| 文档报告质量 | 10% | 文档完整，结构清晰，表达准确 |

**六、参考文献**

1. Scikit-learn官方文档

2. TensorFlow官方指南

3. 《Python机器学习》- Sebastian Raschka

4. 《深度学习》- Ian Goodfellow

**七、补充说明 实验1对毕业要求的支持说明**

**1. 对毕业要求12.1的支持：**

要求内容：“能够正确认识计算机学科和行业的前沿与发展趋势，建立自主学习和终身学习的意识。”

**支持方式：**

实验1要求学生搭建并测试包括深度学习在内的多种机器学习算法环境（如TensorFlow、PyTorch），接触当前AI领域的主流工具与框架。

通过实现、比较不同算法性能，学生了解机器学习技术的最新发展与应用趋势。

实验任务强调自主环境配置、代码调试与结果分析，培养学生的自主学习能力和持续学习意识。

**2. 对毕业要求7.2的间接支持：**

要求内容：“能够评价计算机应用领域复杂工程问题的实践活动对社会可持续发展和环境造成的影响。”

**支持方式：**

实验1虽以技术实现为主，但学生在比较不同算法性能、资源消耗和跨平台表现时，可初步思考算法效率、计算资源与环境影响之间的关系。

通过撰写实验报告中的“性能分析”与“跨平台比较”，学生开始形成对技术实践活动中资源使用与社会成本的基本认识。